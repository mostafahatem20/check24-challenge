import React from 'react';
import Layout from '../components/Layout/Layout';
import UpdatePanel from '../features/UpdatePanel/UpdatePanel';

const Admin = () => {
  return (
    <Layout>
      <UpdatePanel />
    </Layout>
  );
};

export default Admin;
