{
  "name": "@not-so-software/monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.4.2"
  },
  "scripts": {
    "start": "npx lerna run start",
    "shared": "lerna run shared"
  }
}
